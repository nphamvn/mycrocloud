name: Test

on:
  workflow_dispatch:
  pull_request:

jobs:
    main:
        runs-on: ubuntu-latest
        steps:
            - name: Debug
              run: |
                pwd
            
            - name: Checkout
              uses: actions/checkout@v4

            - name: Debug
              run: |
                pwd
                ls -la