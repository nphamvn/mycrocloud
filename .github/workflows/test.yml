name: Test

on:
    workflow_dispatch:

jobs:
    PrintEnvAndSecrets:
        runs-on: ubuntu-latest
        steps:
            - name: Print Environment Variables
              run: |
                echo "Environment Variables"
                print env.SSH_HOST
