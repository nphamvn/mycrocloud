@using MockServer.Core.Identity
@using MockServer.Web.Controllers
@using MockServer.Web.Extentions
@model MockServer.Web.Models.WebApplications.WebApplication
<div class="mb-2">
    <span>
        <a class="text-decoration-none"
            href="@Url.Action(nameof(HomeController.Index), "Home")">@(User.Parse<User>().Username)</a>
    </span>
    <span>/</span>
    <strong><a class="text-decoration-none"
            href="@Url.Action(nameof(WebApplicationsController.View), new {ProjectName=Model.Name})">@Model.Name</a>
    </strong>
</div>